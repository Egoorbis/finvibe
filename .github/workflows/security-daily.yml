name: Daily Security Assessment

# Run daily at 2 AM UTC
on:
  schedule:
    - cron: "0 2 * * *"
  # Allow manual trigger
  workflow_dispatch:

permissions:
  contents: write

jobs:
  security-assessment:
    uses: Egoorbis/security_agent/.github/workflows/security-agent.yml@create_agent
    with:
      timeout-minutes: 15
      report-path: "security-reports/daily-assessment.md"
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
