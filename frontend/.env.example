# API Configuration
# For local development (without Docker)
VITE_API_URL=http://localhost:3000/api

# NOTE: When running with Docker Compose, this value is set
# automatically during the build process via docker-compose.yml
